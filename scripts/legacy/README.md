# Legacy scripts

These are the original versions of `summarize-article.sh` and `batch-process.sh` from the first release of the starter kit. They still work, but Module 3 now teaches a staged pipeline pattern (fetch → clean → process → save). See `../pipeline/` for the current versions.

Keeping these here so you can compare the before and after.
